# meta-repo-orchestrator-test-middle

**Test fixture for AeroTac.** Generated by `tools/spikes/forge/seed.sh`.

Nothing here is product code. This repository exists so the A-7 forge
integration has something real to write against - branches, tags and
pull requests are created and deleted here by test runs.

Role in the fixture composite: `middle`.

```
base  <-  middle  <-  app
```
